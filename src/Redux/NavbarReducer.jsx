const NavbarItems ={
       firstNav:[],
       
}

const NavbarReducer = (state = products, action) => {
       switch (action.type) {

              default: return state
       }
}

export default NavbarReducer